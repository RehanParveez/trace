from app.modules.identity.enums import PermissionKey

RETENTION_PERMISSIONS = {
  PermissionKey.RETENTION_READ: "View retention held by clients and from subcontractors.",
  PermissionKey.RETENTION_MANAGE: "Record retention releases.",
}